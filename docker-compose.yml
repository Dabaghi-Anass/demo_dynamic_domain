version: '3.8'

services:
  app:
    build: .
    container_name: node_app
    restart: always

  nginx:
    image: nginx:latest
    ports:
      - '3050:80'
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
