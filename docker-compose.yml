version: '3.8'
services:
  fallback-app:
    image: nginx:alpine
    volumes:
      - ./404.html:/usr/share/nginx/html/404.html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - coolify
